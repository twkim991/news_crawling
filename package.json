{
  "name": "news_crawling",
  "private": true,
  "description": "Python-based news crawling and classification pipelines.",
  "scripts": {
    "setup": "python -m pip install -r requirements.txt",
    "check:imports": "python -c \"import dotenv, joblib, numpy, pandas, requests, sentence_transformers, sklearn, torch\""
  }
}
